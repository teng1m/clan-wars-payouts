// Edit me freely. Loaded by index.html via <script src="/static/app.js">.
const target = document.getElementById("js-check");
if (target) {
  target.textContent = `JS loaded at ${new Date().toLocaleTimeString()}.`;
}
